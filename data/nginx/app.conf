
upstream certbot {
  server 127.0.0.1:8000;
}
server {
  listen 80;
  server_name _;
location /.well-known/acme-challenge/ {
    root /var/www/certbot;
}
  location /.well-known/acme-challenge {
    proxy_pass http://certbot;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  
    server_tokens off;
    error_log /var/log/nginx/error.log debug;
    access_log /var/log/nginx/access.log;
   }
}
server {
    listen 80;
    server_name adlr.ir;
    # Laravel web root directory
    root /var/www/html/public;
    index index.php index.html;
    location / {
         try_files $uri $uri/ /index.php$is_args$args;
    }

    location ~ \.php$ {
   resolver 127.0.0.1 valid=30s;
        #try_files $uri /index.php =404;
        fastcgi_pass nginx-certbot_adlr.ir_1:9000;
        #fastcgi_index index.php;
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
        fastcgi_param SCRIPT_FILENAME /var/www/html/public$fastcgi_script_name;
        #fixes timeouts
        fastcgi_read_timeout 600;
        include fastcgi_params;
    }
}


      #include conf.d/xn--pgbanj0j0y.com.conf;
      #include conf.d/medrdr.com.conf;
      #include conf.d/medrdr.ir.conf;
     # include conf.d/mydrdr.ir.conf;
      #include conf.d/other/*.conf;
     # include conf.d/apidb.conf;

server {
    listen 80;
    server_name pmmin60.ir;
    root /var/www/pmin60;
    index index.php index.html;

    location ~ \.php$ {
        try_files $uri /index.php =404;
        fastcgi_pass wordpress-docker-compose-wp-1:80;
        fastcgi_index index.php;
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
        #fixes timeouts
        fastcgi_read_timeout 600;
        include fastcgi_params;
    }
}
server {
listen 443;
    server_name abhteam.ir;
    root /var/www/steam/public;
    index index.php index.html;

    location ~ \.php$ {
        #try_files $uri /index.php =404;
        #fastcgi_index index.php;
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
        fastcgi_param SCRIPT_FILENAME /var/www/html/public$fastcgi_script_name;
        #fixes timeouts
        fastcgi_read_timeout 600;
        include fastcgi_params;
    proxy_read_timeout 300s;
    proxy_connect_timeout 75s;
	#include fastcgi_common;
        include fastcgi_params;
        #fastcgi_param QUERY_STRING $query_string;
    	fastcgi_param CONTENT_TYPE $content_type;
    	fastcgi_param CONTENT_LENGTH $content_length;
        fastcgi_pass steam-app-1:8000;
        #proxy_pass http://steam-app-1:8000;
        #fastcgi_buffers 16 16k;
        #fastcgi_buffer_size 32k;
        ##fixes timeouts
    }
}
